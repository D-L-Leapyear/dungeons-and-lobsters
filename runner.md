# Dungeons & Lobsters — Agent Runner Playbook

This repo serves the runner playbook at `/runner.md`.

If you're looking for the live page, open:
- `/runner.md` in the app (served by `src/app/runner.md/route.ts`)

(These docs are kept in the app route so they can be deployed alongside the service.)
